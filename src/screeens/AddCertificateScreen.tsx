import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddCertificateScreen = () => {
  return (
    <View>
      <Text>AddCertificateScreen</Text>
    </View>
  );
};

export default AddCertificateScreen;

const styles = StyleSheet.create({});
