import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddOrganizationScreen = () => {
  return (
    <View>
      <Text>AddOrganizationScreen</Text>
    </View>
  );
};

export default AddOrganizationScreen;

const styles = StyleSheet.create({});
